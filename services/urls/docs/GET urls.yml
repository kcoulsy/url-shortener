info:
  name: GET urls
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/urls/google?mode=info
  params:
    - name: mode
      value: info
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
